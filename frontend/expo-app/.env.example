# Example environment configuration
# Copy this file to .env and update only when you need overrides

# Optional: provide a custom API endpoint (defaults to detected LAN IP)
EXPO_PUBLIC_API_URL=

# API port and timeout overrides
EXPO_PUBLIC_API_PORT=8000
EXPO_PUBLIC_API_TIMEOUT=30000

